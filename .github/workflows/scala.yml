name: Scala CI

on:
  pull_request:
    branches: [ 1.6.x ]
    types: [closed]

jobs:
  image-creation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: trigger single Job
        with:
          repo: "vivekmishratest291/lagom-samples"
          token: "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"
      - run: echo "jenkins"
