name: Scala CI

on:
  pull_request:
    branches: [ 1.6.x ]
    types: [closed]

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      name: trigger single Job
      with:
        url: "http://127.0.0.1:8080/job/devGitPullLagomSample3/build?token=ACTION_SEC"
        user: "vivekmishratest291"
        token: ${ secrets.ACTION_SEC }
        job: "devGitPullLagomSample3"
