name: Scala CI

on:
  pull_request:
    branches: [ 1.6.x ]
    types: [closed]

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      name: trigger single Job
      with:
        url: "http://127.0.0.1:8080/job/devGitPullLagomSample3/build?token=firstGithub"
        user: "vivekmishra91test"
        token: "11f4b5bab68eafb6518f1ea7eedb733d93"
        job: "devGitPullLagomSample3"
